fn main() -> () {
    fn sub(a: i64, b: i64) {
        fn mul(a: i64, b: i64) {()}

        let f = if 3 < 5 {
            5;
        } else {
            ()
        };
    }

    fn add(a: i64, b: i64) {
        let e = sub(a, b);
        let f = if 3 < 5 {
            5;
        } else {
            ()
        };
    }

    let c: bool = true;
}
